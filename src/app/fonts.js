import { Inter, Barlow_Condensed } from "next/font/google";

export const inter = Inter({ subsets: ["latin"], display: "swap" });

export const barlowreg = Barlow_Condensed({
	subsets: ["latin"],
	display: "swap",
	weight: "400",
});

export const barlowbold = Barlow_Condensed({
	subsets: ["latin"],
	display: "swap",
	weight: "700",
});
